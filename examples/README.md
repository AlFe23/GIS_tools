# Examples

This directory will contain sample workflows and notebooks that demonstrate how to use the utilities in `scripts/`.

- `zarr_to_geotiff.ipynb` (coming soon): Walkthrough for converting Zarr datasets into GeoTIFF rasters using the provided script.
